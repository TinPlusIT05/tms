openerp.field_secure = function(instance){

    // make a change in view list
    openerp_field_secure_view_list(instance);

    // make a change in view form
    openerp_field_secure_view_form(instance)
};